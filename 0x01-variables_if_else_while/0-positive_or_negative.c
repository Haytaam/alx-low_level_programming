#include <stdio.h>
/**
 * main - printing the variable n is positive or negative
 * Return: 0 (Success)
 */
int main(void)

{
int n;
if (n > 0)
{
printf("is positive");
return (0);
}
if (n == 0)
{
printf("is zero");
return (0);
}
else
{
printf("is negative");
return (0);
}
return (0);
}

